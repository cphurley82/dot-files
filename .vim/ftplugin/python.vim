" how many columns a tab counts for
set tabstop=4

" hitting tab will insert spaces
set expandtab

" how many columns is indented with reindent operations (<< and >>)
set shiftwidth=4

" how many columns vim uses when you hit Tab in insert mode
set softtabstop=4

